/*
 * 一两货车违反交通规则后逃逸，
 * 现场的目击证人甲说车牌的前两位是相同的
 * 目击证人乙说车牌的后两位是相同的
 * 目击证人丙说四位数的车牌刚好是一个整数的平方
 * 问车牌是什么？
 *
 * 生词   traffic/['træfɪk] n.交通     accident/['æksɪdənt]  n.事故
 */

#include <stdio.h>

int main()
{
  int a,b,c;

  for (a=0; a<=9; a++)
  {
    for (b=0; b<=9; b++)
    {
      for (c=31; c<=99; c++)
      {
        if ((a*1000+a*100+b*10+b == c*c) && (a!=b))
          printf("车牌为: %d%d%d%d\n", a, a, b, b);
      }
    }
  }

  return 0;
}
